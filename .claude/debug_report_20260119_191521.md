# 策略多选功能调试报告

**日期**: 2026-01-19
**问题**: 前端构建失败 - 无法解析 checkbox 组件
**状态**: ✅ 已解决

---

## 问题描述

前端构建时报错：
```
Module not found: Can't resolve '@/components/ui/checkbox'
Import trace: ./app/backtest/new/page.tsx
```

---

## 诊断过程

### Codex 诊断（后端视角）
- **根因**: `@/components` 别名基于 `apps/dashboard` 目录解析，而 `checkbox.tsx` 被错误地放在了仓库根目录
- **建议**: 将组件移动到 `apps/dashboard/components/ui/` 目录

### Gemini 诊断（前端视角）
- **根因**: 
  1. 组件位置错误（超出路径别名范围）
  2. 缺少 `@radix-ui/react-checkbox` 依赖
- **建议**: 移动组件 + 安装依赖

---

## 修复方案

### 1. 移动组件
```bash
# 从错误位置
/Users/ryder/Desktop/workspace/AI/trading_bot/components/ui/checkbox.tsx

# 移动到正确位置
/Users/ryder/Desktop/workspace/AI/trading_bot/apps/dashboard/components/ui/checkbox.tsx
```

### 2. 安装依赖
```bash
npm install @radix-ui/react-checkbox
```

---

## 验证结果

### ✅ 前端构建
```
✓ Compiled successfully
✓ Generating static pages (13/13)
```

### ✅ 后端模块
```
✅ get_weighted_signal 函数导入成功
✅ API 模型导入成功
✅ 回测引擎导入成功
```

---

## 经验总结

1. **路径别名范围**: Next.js 的 `@/` 别名仅指向应用根目录，不能跨越到父级目录
2. **组件位置一致性**: UI 组件应统一放置在 `apps/dashboard/components/ui/` 目录
3. **依赖完整性**: 使用 Radix UI 组件需要安装对应的底层依赖包

---

## 功能状态

| 模块 | 状态 | 说明 |
|------|------|------|
| 后端加权信号函数 | ✅ | strategies/strategies.py |
| 后端API模型 | ✅ | apps/api/models/backtest.py |
| 后端回测引擎 | ✅ | backtest/engine.py |
| 前端状态管理 | ✅ | stores/useBacktestStore.ts |
| 前端多选组件 | ✅ | components/backtest/*.tsx |
| 前端构建 | ✅ | npm run build 通过 |

**策略多选与权重配置功能已完整实现并通过验证！** 🎉
