## Phase 2: 激进优化（中高风险）

**目标**：日收益率 5-7%
**预估工作量**：15 任务点
**风险等级**：高
**前置条件**：Phase 1 稳定运行至少3天，日收益率达到2%+

---

### 模块 E：仓位提升至10%（4 任务点）

**文件**: `config.py`

- [ ] **任务 E.1**：提升基础仓位至10%（1 点）
  - **输入**：Phase 1 配置（5%仓位）
  - **输出**：10%仓位配置
  - **关键步骤**：
    1. `POSITION_SIZE_PERCENT` 从 0.05 提升至 0.10（10%）
    2. 保持 `LEVERAGE = 10`
    3. 有效仓位：10% × 10 = 100%（满仓）
  - **预期收益**：单笔收益翻倍
  - **风险评估**：高（满仓操作，回撤风险大）

- [ ] **任务 E.2**：调整仓位倍数上限（1 点）
  - **输入**：当前仓位倍数配置
  - **输出**：扩大后的倍数范围
  - **关键步骤**：
    1. `MAX_POSITION_MULTIPLIER` 从 2.0 提升至 3.0
    2. `MIN_POSITION_MULTIPLIER` 保持 0.5
    3. Policy Layer 可将仓位放大至 10% × 3.0 = 30%
  - **预期收益**：高胜率时仓位可达300%
  - **风险评估**：极高

- [ ] **任务 E.3**：优化分批建仓策略（1 点）
  - **输入**：当前分批建仓配置
  - **输出**：优化后的建仓策略
  - **关键步骤**：
    1. `POSITION_PARTS` 从 3 降至 2（减少分批次数，加快建仓）
    2. `POSITION_ENTRY_TYPE` 改为 "equal"（均等建仓）
    3. 考虑在强信号时禁用分批（`USE_PARTIAL_POSITION = False`）
  - **预期收益**：建仓速度提升50%
  - **风险评估**：中等

- [ ] **任务 E.4**：调整单日最大亏损限制（1 点）
  - **输入**：当前熔断配置
  - **输出**：放宽后的限制
  - **关键步骤**：
    1. `MAX_DAILY_LOSS_PCT` 从 0.05 提升至 0.10（10%）
    2. 增加警告阈值：7%时发出预警
    3. 确保熔断机制正常工作
  - **预期收益**：避免过早熔断，给系统更多恢复空间
  - **风险评估**：高

---

### 模块 F：止盈扩大至5-8%（3 任务点）

**文件**: `config.py`

- [ ] **任务 F.1**：提升全局止盈至8%（1 点）
  - **输入**：Phase 1 配置（5%止盈）
  - **输出**：8%止盈配置
  - **关键步骤**：
    1. `TAKE_PROFIT_PERCENT` 从 0.05 提升至 0.08（8%）
    2. `TRAILING_STOP_PERCENT` 从 0.04 提升至 0.05（5%）
    3. 保持 `STOP_LOSS_PERCENT = 0.045`
  - **预期收益**：单笔收益提升60%（5%→8%）
  - **风险评估**：中高（止盈过宽可能导致回撤）

- [ ] **任务 F.2**：优化策略级止盈至10%（1 点）
  - **输入**：当前策略配置
  - **输出**：扩大后的策略止盈
  - **关键步骤**：
    1. `multi_timeframe` 止盈从 6% 提升至 10%
    2. `adx_trend` 止盈从 5% 提升至 8%
    3. 其他策略使用全局8%止盈
  - **预期收益**：高质量策略收益最大化
  - **风险评估**：中等

- [ ] **任务 F.3**：调整止损至6%（1 点）
  - **输入**：当前止损配置
  - **输出**：放宽后的止损
  - **关键步骤**：
    1. `STOP_LOSS_PERCENT` 从 0.045 提升至 0.06（6%）
    2. 调整 `ATR_STOP_MULTIPLIER` 从 4.0 提升至 5.0
    3. 更新策略级止损配置
  - **预期收益**：减少被止损次数，提升胜率
  - **风险评估**：高（单笔亏损增加）

---

### 模块 G：降低策略阈值（4 任务点）

**文件**: `config.py`

- [ ] **任务 G.1**：进一步降低共识信号阈值（1 点）
  - **输入**：Phase 1 配置
  - **输出**：更低的阈值
  - **关键步骤**：
    1. `MIN_STRATEGY_AGREEMENT` 从 0.35 降至 0.30
    2. `MIN_SIGNAL_STRENGTH` 从 0.40 降至 0.35
    3. `MIN_SIGNAL_CONFIDENCE` 从 0.30 降至 0.25
  - **预期收益**：交易频率再提升30%
  - **风险评估**：高（假信号增加）

- [ ] **任务 G.2**：禁用ML信号过滤器（1 点）
  - **输入**：当前ML配置
  - **输出**：禁用ML过滤
  - **关键步骤**：
    1. `ML_MODE` 从 "shadow" 改为 "off"
    2. 或降低 `ML_QUALITY_THRESHOLD` 从 0.35 到 0.20
    3. 记录禁用前后的信号通过率
  - **预期收益**：信号通过率提升20-30%
  - **风险评估**：中高

- [ ] **任务 G.3**：禁用Claude信号分析（1 点）
  - **输入**：当前Claude配置
  - **输出**：禁用Claude过滤
  - **关键步骤**：
    1. `ENABLE_CLAUDE_ANALYSIS` 改为 False
    2. 或降低 `CLAUDE_MIN_SIGNAL_STRENGTH` 从 0.3 到 0.1
    3. 保留Claude定期分析和每日报告
  - **预期收益**：信号通过率提升10-15%
  - **风险评估**：中等（失去AI质量把关）

- [ ] **任务 G.4**：启用更多策略（1 点）
  - **输入**：当前策略列表
  - **输出**：扩展后的策略列表
  - **关键步骤**：
    1. 启用 `volume_breakout`（捕获突破行情）
    2. 启用 `kdj_cross`（增加信号来源）
    3. 考虑启用 `bollinger_breakthrough`（抄底策略）
  - **预期收益**：交易机会增加40%
  - **风险评估**：中高

---

### 模块 H：启用Policy Layer激进模式（4 任务点）

**文件**: `config.py`, `policy_layer.py`

- [ ] **任务 H.1**：启用Policy Layer（1 点）
  - **输入**：当前Policy配置
  - **输出**：启用Policy Layer
  - **关键步骤**：
    1. `ENABLE_POLICY_LAYER` 改为 True
    2. `POLICY_LAYER_MODE` 保持 "active"
    3. `POLICY_ANALYZE_ON_STARTUP` 改为 True
  - **预期收益**：动态参数优化
  - **风险评估**：中等

- [ ] **任务 H.2**：扩大Policy参数边界（1 点）
  - **输入**：当前参数边界
  - **输出**：扩大后的边界
  - **关键步骤**：
    1. `stop_loss_pct` 上限从 0.05 提升至 0.08（8%）
    2. `take_profit_pct` 上限从 0.10 提升至 0.15（15%）
    3. `position_multiplier` 上限从 2.0 提升至 3.0
  - **预期收益**：Policy可做更激进调整
  - **风险评估**：高

- [ ] **任务 H.3**：调整风控模式切换阈值（1 点）
  - **输入**：当前风控模式配置
  - **输出**：优化后的切换规则
  - **关键步骤**：
    1. `POLICY_DEFENSIVE_LOSS_THRESHOLD` 从 3 提升至 5（更晚触发防守）
    2. `POLICY_AGGRESSIVE_WIN_THRESHOLD` 从 5 降至 3（更早触发激进）
    3. 调整激进模式参数倍数
  - **预期收益**：更快进入激进模式
  - **风险评估**：高

- [ ] **任务 H.4**：优化Policy激进模式参数（1 点）
  - **输入**：`policy_layer.py` 中的风控模式参数
  - **输出**：更激进的参数
  - **关键步骤**：
    1. 激进模式止损倍数从 1.2 提升至 1.5
    2. 激进模式止盈倍数从 1.5 提升至 2.0
    3. 激进模式仓位倍数从 1.3 提升至 2.0
  - **预期收益**：激进模式下收益翻倍
  - **风险评估**：极高

---

**Phase 2 预期成果：**
- 日收益率：5-7%
- 交易频率：10-15次/天
- 胜率要求：45%+
- 最大回撤：15-20%
- **警告**：此阶段风险显著增加，建议小资金测试

---

